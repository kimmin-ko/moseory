<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.moseory.mapper.MemberMapper">

	<resultMap type="MemberVO" id="memberResultMap">
		<result column="no" property="no" />
		<result column="id" property="id" />
		<result column="password" property="password" />
		<result column="pwd_confirm_q" property="pwd_confirm_q" />
		<result column="pwd_confirm_a" property="pwd_confirm_a" />
		<result column="name" property="name" />
		<result column="zipcode" property="zipcode" />
		<result column="address1" property="address1" />
		<result column="address2" property="address2" />
		<result column="tel" property="tel" />
		<result column="phone" property="phone" />
		<result column="email" property="email" />
		<result column="birth" property="birth" />
		<result column="lev" property="lev" typeHandler="LevelEnumTypeHandler" />
		<result column="point" property="point" />
		<result column="total" property="total" />
		<result column="join_date" property="join_date" />
	</resultMap>
	
	<insert id="insertMember">
	insert into tbl_member (no, id, password, pwd_confirm_q, pwd_confirm_a , name, 
	zipcode, address1, address2, tel, phone, email, birth, lev, point, total, join_date)
	values (seq_member_no.nextval, #{id}, #{password}, #{pwd_confirm_q}, #{pwd_confirm_a}, #{name}, 
	#{zipcode}, #{address1}, #{address2}, #{tel}, #{phone}, #{email}, #{birth}, #{lev}, #{point}, #{total}, #{join_date})
	</insert>
	
	<delete id="deleteAll">
	delete from tbl_member
	</delete>
	
	<select id="getCountMember" resultType="int">
	select count(*) from tbl_member where id = #{id}
	</select>
	
	<select id="getCount" resultType="int">
	select count(*) from tbl_member
	</select>
	
	<select id="getMember" resultMap="memberResultMap">
	select no, id, password, pwd_confirm_q, pwd_confirm_a , name, zipcode, address1, address2, 
	tel, phone, email, birth, lev, point, total, join_date
	from tbl_member
	where id = #{id}
	</select>
	
	<delete id="deleteMember">
	delete from tbl_member where id = #{id}
	</delete>
	
</mapper>